#ifndef _SYNTHESISNORMALIZER_XML_SYNTHESISNORMALIZER_CMPT_
#define _SYNTHESISNORMALIZER_XML_SYNTHESISNORMALIZER_CMPT_
/******************** generated by xml-casa (v2) from synthesisnormalizer.xml *******
********************* e7758c1bfca4799c53d8fddadd53d3e4 *****************************/

#include <vector>
#include <string>
#include <complex>
#include <stdcasa/record.h>
#include <casaswig_types.h>
#include <casa/Exceptions/Error.h>
#include <synthesisnormalizer_forward.h>

#include <synthesisimstore_cmpt.h>


using namespace std;

namespace casac {

  class  synthesisnormalizer  {
    public:

      synthesisnormalizer();
      bool setupnormalizer(const record& _normpars=initialize_record(""""));
      bool gatherweightdensity();
      bool scatterweightdensity();
      bool gatherpsfweight();
      bool gatherresidual();
      bool dividepsfbyweight();
      bool normalizeprimarybeam();
      bool divideresidualbyweight();
      bool dividemodelbyweight();
      bool multiplymodelbyweight();
      bool scattermodel();
      casac::synthesisimstore* getimstore();
      bool setimstore(const casac::synthesisimstore* _imstore=0);
      bool done();

        ~synthesisnormalizer( );

    private:

#include <synthesisnormalizer_private.h>


      // --- declarations of static parameter defaults ---
    public:

  };

}

#endif
