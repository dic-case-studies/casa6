/******************** generated by xml-casa (v2) from imager.xml ********************
********************* cdfa978e12a3db4a421bf7d1347680e3 *****************************/
#include <vector>
#include <string>
#include <complex>
#include <stdcasa/record.h>
#include <casaswig_types.h>
#include <imager_forward.h>

#include <imager_cmpt.h>

using namespace std;

namespace casac {

  int imager::_pixels_advise=int(0);
  record imager::_cell_advise=initialize_record("""");
  int imager::_facets_advise=int(0);
  string imager::_phasecenter_advise=string("");
  record imager::_bmaj_fitpsf=initialize_record("""");
  record imager::_bmin_fitpsf=initialize_record("""");
  record imager::_bpa_fitpsf=initialize_record("""");
  record imager::_pointsource_sensitivity=initialize_record("""");
  double imager::_relative_sensitivity=double(0.0);
  double imager::_sumweights_sensitivity=double(0.0);
  record imager::_senrec_sensitivity=initialize_record("""");
  double imager::_pointsource_apparentsens=double(0.0);
  double imager::_relative_apparentsens=double(0.0);

}
